const buttons = document.querySelectorAll("button");

buttons.addEventListener("click", function () {
    const valorBotao = buttons.innerHTML;
    console.log(valorBotao);
})

//console.log(button);

function somar() {
    if (buttons += 0) {
        console.log(buttons);
    }
}